## 1. 项目基础与应用骨架

- [x] 1.1 创建 Tauri 2 + React + TypeScript + Tailwind CSS 桌面应用基础结构
- [x] 1.2 配置 Zustand 状态管理和前端基础目录结构
- [x] 1.3 配置 Rust 侧 axum、tokio、HTTP 客户端、UUID、本地配置和文件系统相关依赖
- [x] 1.4 建立 Tauri command 与 Tauri event 的前后端通信约定
- [x] 1.5 定义 v0.1 协议版本、应用版本、任务状态、设备信息、最近连接和文件队列的数据类型
- [x] 1.6 建立 Rust 与前端代码规范：模块职责清晰、命名一致、类型集中管理、错误处理可追踪，避免临时性堆叠代码
- [x] 1.7 建立注释规范：Rust 侧对关键 API、异步/流式处理、Tauri command/event、axum handler、错误处理和协议约定补充学习型注释；前端侧对复杂状态推导和交互边界补充必要注释

## 2. 本机设备与接收服务

- [x] 2.1 实现首次启动生成并持久化 `deviceId`
- [x] 2.2 实现系统设备名读取、自定义设备名保存和兜底设备名展示，自定义设备名最大 256 个字符且去除首尾空白后不能为空
- [x] 2.3 实现本机网络接口枚举、IPv4 过滤、私有地址优先和虚拟网卡降权推荐
- [x] 2.4 实现候选 IP 列表、推荐地址展示、手动切换展示地址和复制访问地址
- [x] 2.5 实现本地 HTTP 服务监听 `0.0.0.0`，默认端口 `7788`，冲突时递增尝试到 `7888`
- [x] 2.6 实现服务启动失败、端口不可用和服务状态展示
- [x] 2.7 实现 `GET /api/device` 返回设备信息、版本、协议版本和 `receiveEnabled`

## 3. 保存目录与接收可用性

- [x] 3.1 实现默认保存目录为系统下载目录下 `LanTransfer`
- [x] 3.2 实现用户通过系统目录选择器修改保存目录并持久化
- [x] 3.3 实现保存目录不存在时自动创建
- [x] 3.4 实现保存目录不可创建、不可访问或不可写时的错误提示
- [x] 3.5 将保存目录可用性同步到 `receiveEnabled`
- [x] 3.6 实现打开当前保存目录操作

## 4. 主窗口 UI

- [x] 4.1 按 `UI/design.png` 实现单主页面三栏工作台布局
- [x] 4.2 实现左侧本机状态、网络候选、保存目录和底部连接摘要
- [x] 4.3 实现中间目标设备连接、最近连接列表、文件拖拽区、选择文件入口和待发送列表
- [x] 4.4 实现右侧传输任务列表、筛选标签、状态标记、进度展示和空状态
- [x] 4.5 按 `UI/confirm.png` 实现接收文件请求确认弹窗或确认区
- [x] 4.6 实现基础响应式与窗口尺寸约束，确保主页面文本和控件不重叠

## 5. 目标连接与最近连接

- [x] 5.1 实现目标地址输入格式校验和 HTTP 地址规范化
- [x] 5.2 实现连接测试调用目标设备 `GET /api/device`
- [x] 5.3 实现连接成功后的目标设备信息展示和当前发送目标更新
- [x] 5.4 实现地址错误、请求超时、连接拒绝、返回内容不匹配、协议不兼容和对方不可接收的提示
- [x] 5.5 实现连接测试成功后保存或更新最近连接设备
- [x] 5.6 实现最近连接按最近成功时间倒序、最多保留 10 条
- [x] 5.7 实现点击最近连接填入地址、删除单条记录和重新连接测试
- [x] 5.8 实现存在等待确认或传输中发送任务时锁定目标连接区

## 6. 文件选择与待发送队列

- [x] 6.1 实现系统文件选择器选择一个或多个文件
- [x] 6.2 实现拖拽文件到发送区域并加入待发送列表
- [x] 6.3 实现拒绝文件夹作为 v0.1 待发送项
- [x] 6.4 实现待发送文件名、单文件大小、文件数量和总大小展示
- [x] 6.5 实现发送前从待发送列表移除文件
- [x] 6.6 实现待发送列表为空或目标未连接时禁用发送
- [x] 6.7 实现发送前校验文件存在且可读取

## 7. 传输请求与接收确认

- [x] 7.1 实现发送方提交 `POST /api/transfer/request` 元数据
- [x] 7.2 实现接收方空闲时创建 pending 任务并生成 `transferId`
- [x] 7.3 实现接收方忙碌时返回 `receiver_busy`
- [x] 7.4 实现 Rust 侧通过 Tauri event 通知本机 React 展示接收确认
- [x] 7.5 实现接收确认展示发送方、地址、文件数量、总大小和文件明细
- [x] 7.6 实现接收方接收或拒绝整个传输任务
- [x] 7.7 实现发送方轮询 `GET /api/transfer/status/:transferId`，间隔约 1 秒
- [x] 7.8 实现 pending、accepted、rejected、uploading、completed、failed 状态响应

## 8. 文件上传与保存

- [x] 8.1 实现接收方确认后发送方按待发送列表顺序逐个上传文件
- [x] 8.2 实现 `POST /api/transfer/upload` 关联 `transferId` 和当前文件
- [x] 8.3 实现 Rust 侧流式读取文件并通过 HTTP 上传
- [x] 8.4 实现接收方流式接收文件并写入保存目录
- [x] 8.5 实现接收前检查重名文件并展示重名确认
- [x] 8.6 实现覆盖全部重名文件或取消整个传输任务
- [x] 8.7 实现任一文件上传或保存失败时整个任务失败
- [x] 8.8 实现连接中断、应用关闭、服务异常和磁盘空间不足等基础错误处理

## 9. 传输任务状态与进度

- [x] 9.1 实现运行期发送任务和接收任务列表，应用重启后不保留历史
- [x] 9.2 实现任务方向展示为“发送给”或“从某设备接收”
- [x] 9.3 实现等待确认、已拒绝、传输中、成功、失败状态流转
- [x] 9.4 实现基于已传输字节和总字节的整体任务进度
- [x] 9.5 实现 Rust 侧进度事件同步到 React/Zustand
- [x] 9.6 实现接收成功后打开保存目录
- [x] 9.7 实现清空已完成任务

## 10. 验证与验收

- [x] 10.1 在单机环境验证本地服务启动、端口冲突递增和 `/api/device`
- [ ] 10.2 在两台同局域网设备验证手动输入地址、连接测试和最近连接
- [ ] 10.3 验证单文件发送、接收确认、保存和打开目录
- [ ] 10.4 验证多文件顺序发送、整体进度和任务最终状态
- [ ] 10.5 验证接收方拒绝、接收方忙碌、地址错误、连接失败和协议不兼容提示
- [ ] 10.6 验证保存目录不可用、重名覆盖确认和保存失败提示
- [ ] 10.7 对照 `docs/PRD-v0.1.md` 的验收标准逐项确认 v0.1 范围完成
- [x] 10.8 进行代码可读性检查：确认 Rust 和前端模块边界清晰、核心类型命名统一、错误信息可定位、无明显重复或临时代码
- [x] 10.9 进行注释质量检查：确认 Rust 关键学习点已有说明，注释解释调用规则、注意点和设计原因，而不是简单复述代码
